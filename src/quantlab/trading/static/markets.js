/* Environment controls and presentation. Python owns executions and all financial values. */
(()=>{'use strict';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const n=v=>v===null||v===undefined?'Unavailable':typeof v==='number'?v.toLocaleString('en-GB',{maximumFractionDigits:5}):String(v);
const money=v=>v===null||v===undefined?'Unavailable':'£'+n(Number(v));
let catalog,token,busy=false,selectedState=null,lastRevision=null;
const main=document.querySelector('main');if(!main)return;
const bar=document.createElement('div');bar.className='env-bar';bar.innerHTML='<strong id="env-badge">SYNTHETIC</strong><span id="env-source-note">Controlled artificial market</span><a href="/#choose-environment">Choose environment</a>';document.querySelector('.platform-nav')?.after(bar);
const chooser=document.createElement('details');chooser.className='env-chooser';chooser.id='choose-environment';chooser.innerHTML=`<summary>Choose your market environment</summary><div class="env-cards">
<section class="env-card"><h2>Synthetic Market</h2><p>Controlled artificial market where QuantLab knows the generating process.</p><small>Real: your instructions.<br>Simulated: agents, value process and trading.<br>Execution: actual orders match in the simulated FIFO book.</small><button id="env-synthetic" type="button">Use synthetic market</button></section>
<section class="env-card"><h2>Historical Replay</h2><p>Trade progressively through imported recorded observations with paper money.</p><small>Real: recorded data when supported by its provenance.<br>Simulated: every fill and account.<br>QuantLab knows the revealed path, not why the real market moved.</small><button id="env-history" type="button">Set up historical replay</button></section>
<section class="env-card"><h2>Scenario Market</h2><p>Explore a deliberately configured synthetic regime, known or hidden.</p><small>Real: your choices.<br>Simulated: the configured regime and executions.<br>Hidden setup stays private until an explicit post-session reveal.</small><button id="env-scenario" type="button">Set up a scenario</button></section></div>
<div id="env-setup" class="env-form" hidden></div><div class="env-status" id="env-status" role="status"></div>`;main.prepend(chooser);
async function get(){const r=await fetch('/api/environments');const d=await r.json();if(!r.ok)throw Error(d.error||'Environment unavailable');return d;}
async function command(kind,payload={}){if(!token)token=(await(await fetch('/api/bootstrap')).json()).token;const r=await fetch('/api/environments',{method:'POST',headers:{'Content-Type':'application/json','X-QuantLab-Token':token},body:JSON.stringify({kind,payload})});const d=await r.json();if(!r.ok||d.result?.ok===false)throw Error(d.error||d.result?.message||'Environment action failed');return d;}
async function act(kind,payload={},reload=false){if(busy)return;busy=true;const status=document.getElementById('env-status');status.textContent='Checking the action and public evidence…';try{const out=await command(kind,payload);status.textContent=out.result?.message||'Ready';if(reload){location.href='/';return;}if(out.state){catalog=out;render(out);}return out;}catch(e){status.textContent=e.message;const deskStatus=document.getElementById('env-desk-status');if(deskStatus)deskStatus.textContent=e.message;}finally{busy=false;}}
function field(id,label,value,type='text'){return `<label class="env-field" for="${id}">${label}<input id="${id}" type="${type}" value="${esc(value)}" ${type==='number'?'step="any"':''}></label>`;}
function datasetOptions(){return catalog.datasets.map(d=>`<option value="${esc(d.id)}">${esc(d.instrument)} · ${d.data_kind==='artificial_fixture'?'ARTIFICIAL FIXTURE':'imported recorded data'}</option>`).join('');}
function historySetup(){const target=document.getElementById('env-setup');target.hidden=false;target.innerHTML=`<h3>Import a local OHLCV session</h3><p class="env-note">GBP only in this version. CSV columns: timestamp, open, high, low, close, volume. Timestamps are bar-end availability times with explicit timezone offsets. No quotes, book depth or option chains are invented.</p>
<form id="env-import"><label class="env-field">Historical CSV<input type="file" id="env-csv" accept=".csv,text/csv" required></label><div class="env-row">${field('env-instrument','Instrument','')}${field('env-data-source','Data source','')}${field('env-licence','Provenance / local-use rights note','')}</div><div class="env-row">${field('env-timezone','IANA timezone','UTC')}${field('env-frequency','Frequency (seconds)','60','number')}${field('env-tick','Price tick (£)','0.01')}</div><div class="env-row"><label class="env-field">Adjustment basis<select id="env-adjust"><option value="raw">Raw · no corporate actions in this session</option><option value="adjusted">All OHLC consistently adjusted</option></select></label>${field('env-transform','Transformations','None')}</div><p class="env-warning">Confirm the provider’s adjustment basis and corporate actions. Conflicting adjustment declarations are rejected; consistent numbers alone do not prove that every field was adjusted. Importing data does not certify its source or grant redistribution rights.</p><button type="submit">Validate and import CSV</button></form>
<hr><h3>Or try the mechanics with artificial data</h3><p class="env-note">The bundled fixture is generated test data. It is NOT real market history.</p><button id="env-fixture" type="button">Load artificial test fixture</button><hr>
<form id="env-start-history"><label class="env-field">Imported instruments · select one or two<select multiple size="3" id="env-datasets">${datasetOptions()}</select></label><div class="env-row">${field('env-participation','Volume fraction · 0.10 = 10%','0.10')}${field('env-slip','Adverse slippage (ticks)','1','number')}${field('env-fee','Fee per unit (£)','0.001')}</div><p class="env-note">Market instructions wait for the next revealed close. A shared volume budget can limit fills. Unfilled market remainder cancels; limit remainder may wait. High/low never triggers fills.</p><button type="submit">Start historical paper replay</button></form>`;
target.querySelector('#env-import').onsubmit=async e=>{e.preventDefault();const file=target.querySelector('#env-csv').files[0];if(!file)return;const basis=target.querySelector('#env-adjust').value;const out=await act('import',{csv_text:await file.text(),metadata:{instrument:target.querySelector('#env-instrument').value,source:target.querySelector('#env-data-source').value,licence:target.querySelector('#env-licence').value,timezone:target.querySelector('#env-timezone').value,frequency_seconds:Number(target.querySelector('#env-frequency').value),tick_size:target.querySelector('#env-tick').value,currency:'GBP',price_basis:basis,corporate_actions:basis==='raw'?'none_in_session':'all_ohlc_adjusted',transformations:target.querySelector('#env-transform').value,data_kind:'recorded'}});if(out){catalog=out;target.querySelector('#env-datasets').innerHTML=datasetOptions();target.querySelector('#env-datasets').value=out.result.dataset_id;}};
target.querySelector('#env-fixture').onclick=async()=>{const out=await act('fixture');if(out){catalog=out;target.querySelector('#env-datasets').innerHTML=datasetOptions();for(const o of target.querySelector('#env-datasets').options)o.selected=out.result.dataset_ids.includes(o.value);}};
target.querySelector('#env-start-history').onsubmit=e=>{e.preventDefault();act('choose',{environment:'HISTORICAL',dataset_ids:[...target.querySelector('#env-datasets').selectedOptions].map(o=>o.value),execution:{participation:target.querySelector('#env-participation').value,slippage_ticks:Number(target.querySelector('#env-slip').value),fee_per_unit:target.querySelector('#env-fee').value}},true);};}
function scenarioSetup(){const target=document.getElementById('env-setup');target.hidden=false;target.innerHTML=`<h3>Configure a synthetic scenario</h3><form id="env-start-scenario"><label><input type="checkbox" id="env-hidden"> Hidden scenario · choose privately on this computer</label><div id="env-known-fields"><label class="env-field">Known scenario<select id="env-scenario-choice">${catalog.scenarios.map(s=>`<option value="${esc(s.id)}">${esc(s.title)} · ${esc(s.engine)}</option>`).join('')}</select></label><p class="env-note" id="env-mechanism"></p>${field('env-seed','Reproduction seed','42','number')}</div><div class="env-row">${field('env-duration','Seconds / model observations · 20–120','60','number')}<label class="env-field">Stock-desk profile<select id="env-profile"><option value="free">Manual trading</option><option value="manual_maker">Manual market making · stock scenarios</option></select></label></div><p class="env-note">Multi-asset scenarios use observations; option scenarios use model days. Mean reversion/trend describe the configured relationship, not a promise about every asset. A hidden session reveals neither its name nor future shocks.</p><button type="submit">Start scenario session</button></form>`;
const choose=target.querySelector('#env-scenario-choice');const update=()=>target.querySelector('#env-mechanism').textContent=catalog.scenarios.find(s=>s.id===choose.value).mechanism;choose.onchange=update;update();target.querySelector('#env-hidden').onchange=e=>target.querySelector('#env-known-fields').hidden=e.target.checked;
target.querySelector('form').onsubmit=e=>{e.preventDefault();const hidden=target.querySelector('#env-hidden').checked;act('choose',{environment:'SCENARIO',hidden,steps:Number(target.querySelector('#env-duration').value),mode:target.querySelector('#env-profile').value,...(hidden?{}:{scenario:choose.value,seed:Number(target.querySelector('#env-seed').value)})},true);};}
chooser.querySelector('#env-synthetic').onclick=()=>act('choose',{environment:'SYNTHETIC'},true);chooser.querySelector('#env-history').onclick=historySetup;chooser.querySelector('#env-scenario').onclick=scenarioSetup;
function table(headers,rows){return `<div class="env-table"><table><thead><tr>${headers.map(h=>'<th>'+esc(h)+'</th>').join('')}</tr></thead><tbody>${rows.map(row=>'<tr>'+row.map(v=>'<td>'+esc(n(v))+'</td>').join('')+'</tr>').join('')}</tbody></table></div>`;}
function raw(v){return `<pre class="env-raw">${esc(JSON.stringify(v,null,2))}</pre>`;}
function explainButton(key,label){return `<button type="button" data-env-explain="${key}" ${key==='delta'?'data-env-selector="portfolio"':''}>Explain ${label}</button>`;}
function plot(rows){if(!rows.length)return '<p class="env-note">No public price observations yet.</p>';const values=rows.map(r=>r[1]),lo=Math.min(...values),hi=Math.max(...values),span=hi-lo||1;const points=rows.map((r,i)=>`${45+i/Math.max(1,rows.length-1)*610},${190-(r[1]-lo)/span*145}`).join(' ');return `<svg class="env-plot" viewBox="0 0 700 230" role="img" aria-label="Only revealed prices; scale uses the revealed prefix"><text x="8" y="32" fill="#45646b" font-size="12">£${esc(n(hi))}</text><text x="8" y="194" fill="#45646b" font-size="12">£${esc(n(lo))}</text><polyline points="${points}" fill="none" stroke="#347a75" stroke-width="2.5"/><text x="45" y="218" fill="#45646b" font-size="11">${esc(rows[0][0])}</text><text x="655" y="218" text-anchor="end" fill="#45646b" font-size="11">${esc(rows.at(-1)[0])}</text></svg>`;}
function render(out){catalog=out;const s=out.state;selectedState=s;document.getElementById('env-badge').textContent=s?s.environment.badge:'SYNTHETIC';document.getElementById('env-source-note').textContent=s?s.environment.truth:'Controlled agents, stochastic value and the existing simulated FIFO book';const desk=document.getElementById('environment-desk');if(!desk)return;if(!s){location.href='/';return;}
const revision=[s.revision,s.frame_index,s.status,out.playing,out.speed,out.research?.experiment_id].join(':');if(lastRevision===revision)return;lastRevision=revision;const hist=s.engine==='historical',c=s.core||{},a=hist?s.account:s.engine==='trading'?c.account:c.accounts||c.metrics||{},ended=s.status==='ended',replay=s.replay,readonly=ended||replay;
const pnl=hist?a.pnl:s.engine==='trading'?a.total_pnl:s.engine==='options'?a.total_pnl:a.net_pnl;
const q=hist?s.account.positions.map(p=>p.instrument+': '+p.quantity).join(' · ')||'0':s.engine==='trading'?a.position:s.engine==='options'?c.greeks?.delta:c.exposure?.gross;
const risk=hist?s.risk:s.risk||{};let rows=hist?s.observations[s.instruments[0]].map(b=>[b.timestamp.slice(11,19),b.close]):s.engine==='trading'?(c.tape||[]).map(t=>[(t.time_us/1e6).toFixed(2)+'s',Number(t.price)]):s.engine==='options'?(c.history||[]).map(h=>['Day '+h.step,h.spot]):(c.history||[]).map((p,i)=>[String(i),p[0]]);
const ids=hist?s.instruments:s.engine==='statarb'?(c.markets||[]).map(m=>m.instrument):['QL-STOCK'];const orders=hist?s.orders:s.engine==='statarb'?(c.markets||[]).flatMap(m=>m.orders.map(o=>({...o,instrument:m.instrument}))):c.orders||c.stock?.orders||[];const fills=hist?s.fills:c.trades||c.option_trades||c.fills||[];
desk.innerHTML=`<p class="env-note">${esc(s.environment.badge)} · ${replay?'READ-ONLY VERIFIED REPLAY':esc(s.status.toUpperCase())}</p><h1>${hist?'Trade the revealed path.':esc(s.environment.title)}</h1><p>${esc(s.environment.truth)}</p>${hist?`<p class="env-warning">SIMULATED EXECUTION ON HISTORICAL DATA · ${esc(s.options_note)}</p>`:''}<div id="env-desk-status" class="env-status" role="status">${esc(out.result?.message||s.last_result?.message||'')}</div>
<div class="env-controls"><strong>${hist?esc(s.timestamp):'Observation '+s.index}</strong><button data-env-action="play" ${readonly?'disabled':''}>Play</button><button data-env-action="pause">Pause</button><button data-env-action="step" ${readonly?'disabled':''}>Step observation</button>${hist||s.engine==='trading'?`<button data-env-action="step_time" ${readonly?'disabled':''}>Step ${hist?'1 minute':'1 second'}</button>`:''}<label>Speed <select id="env-speed" aria-label="Playback observations per second">${[1,2,4,10].map(n=>`<option value="${n}" ${n===(out.speed||1)?'selected':''}>${n} observations / second</option>`).join('')}</select></label><button data-env-action="end" ${readonly?'disabled':''}>End session</button><button data-env-action="restart" ${readonly?'':'disabled'}>Start a new session at the beginning</button></div>
${replay?`<div class="env-controls"><button id="env-prev" ${s.frame_index===0?'disabled':''}>Previous frame</button><span>Frame ${s.frame_index+1} / ${s.frame_count}</span><button id="env-next" ${s.frame_index===s.frame_count-1?'disabled':''}>Next frame</button><button id="env-final">Ended frame</button></div>`:''}
<div class="env-metrics"><div class="env-metric"><small>Net account P&L</small><strong>${esc(money(pnl))}</strong>${explainButton('pnl_attribution','P&L')}</div><div class="env-metric"><small>${s.engine==='options'?'Portfolio delta':'Position / exposure'}</small><strong>${esc(n(q))}</strong>${explainButton(s.engine==='options'?'delta':'position','position')}</div><div class="env-metric"><small>Maximum drawdown</small><strong>${esc(money(hist?a.drawdown:s.challenge?.drawdown??a.max_drawdown??a.drawdown))}</strong>${explainButton('drawdown','drawdown')}</div><div class="env-metric"><small>${hist?'Past-return VaR':'Current observation'}</small><strong>${esc(hist?money(risk.var):n(s.index))}</strong>${explainButton(hist?'var':'current_price',hist?'VaR':'price')}</div></div>
${hist?`<section class="env-section"><h2>Account ledger</h2>${table(['Cash','Realised gross P&L','Unrealised P&L','Fees','Net P&L'],[[money(a.cash),money(a.realised_gross),money(a.unrealised),money(a.fees),money(a.pnl)]])}<p class="env-note">Net P&L = realised gross + unrealised − fees. Marks use revealed closes.</p>${table(['Instrument','Current revealed close'],s.instruments.map(k=>[k,money(s.observations[k].at(-1).close)]))}</section>`:''}
<div class="env-grid"><div><section class="env-section"><h2>${hist?'Revealed imported observations':'Public market'}</h2>${plot(rows)}<p class="env-note">${hist?'The chart and its range use only revealed bars. A close is not a bid or ask.':'Prices shown come from the selected existing synthetic engine.'}</p>${explainButton('current_price','current price')}${hist?table(['Time','Open','High','Low','Close','Volume'],s.observations[s.instruments[0]].slice(-6).map(b=>[b.timestamp,b.open,b.high,b.low,b.close,b.volume])):s.engine==='trading'?table(['Side','Price','Quantity','Order count'],['asks','bids'].flatMap(side=>(c[side]||[]).map(p=>[side,p.price,p.quantity,p.order_count]))):''}</section>
<section class="env-section"><h2>${hist?'Paper instructions':'Orders'}</h2>${table(['Order','Instrument','Side','Original','Filled','Remaining','Status'],orders.map(o=>[o.order_id,o.instrument||'',o.side,o.original,o.filled,o.remaining,o.status]))}<label class="env-field">Cancel order<select id="env-cancel-order"><option value="">Choose an open order</option>${orders.map((o,i)=>o.remaining?`<option value="${i}">${esc(o.instrument||'')} ${esc(o.order_id)}</option>`:'').join('')}</select></label><button id="env-cancel" ${readonly?'disabled':''}>Cancel unfilled quantity</button></section>
<section class="env-section"><h2>${hist?'Simulated historical executions':'Actual simulated executions'}</h2>${table(['Time / step','Instrument','Side','Quantity','Price','Fee'],fills.slice(-15).map(f=>[f.timestamp??f.time_us??f.step??f.t,f.instrument??'',f.side,f.quantity,f.price??f.price_gbp,f.fee??'']))}${hist||s.engine==='trading'?explainButton('vwap','VWAP'):explainButton('pnl_attribution','execution accounting')}</section></div>
<aside><section class="env-section"><h2>Order ticket</h2><form id="env-order-form"><label class="env-field">Instrument<select id="env-order-instrument">${ids.map(k=>`<option>${esc(k)}</option>`).join('')}</select></label><div class="env-row"><label class="env-field">Side<select id="env-order-side"><option value="buy">Buy</option><option value="sell">Sell</option></select></label>${field('env-order-quantity','Whole units','5','number')}</div><label class="env-field">Order type<select id="env-order-type"><option value="market">Market</option><option value="limit">Limit</option></select></label>${field('env-order-price','Limit price (£)','100')}<p class="env-note">${hist?'Orders wait until a later revealed close. Market remainder cancels; limit remainder can wait.':'Orders use the selected existing matching/quote engine.'}</p><button type="submit" ${readonly?'disabled':''}>Submit simulated order</button></form></section>
${s.engine==='options'?`<section class="env-section"><h2>Synthetic option</h2><form id="env-option-form"><label class="env-field">Contract<select id="env-contract">${(c.chain||[]).map(p=>`<option value="${esc(p.id)}" ${p.id===c.selected?.id?'selected':''}>${esc(p.type)} £${esc(p.strike)} · ${esc(p.remaining_days)} days · bid £${esc(p.bid)} / ask £${esc(p.ask)} · size ${esc(p.ask_size)}</option>`).join('')}</select></label><label class="env-field">Option side<select id="env-option-side"><option value="buy">Buy</option><option value="sell">Sell</option></select></label>${field('env-contracts','Contracts','1','number')}<button ${readonly?'disabled':''}>Trade synthetic option</button></form><button data-env-action="hedge" ${readonly?'disabled':''}>Execute delta hedge</button>${explainButton('delta','delta')}</section>`:''}
${s.engine==='trading'&&c.mode==='manual_maker'?`<section class="env-section"><h2>Manual quotes</h2><form id="env-quotes"><div class="env-row">${field('env-bid-distance','Bid distance (ticks)','2')}${field('env-ask-distance','Ask distance (ticks)','2')}</div><div class="env-row">${field('env-bid-size','Bid units','2','number')}${field('env-ask-size','Ask units','2','number')}</div>${field('env-shift','Quote shift (ticks)','0')}<button ${readonly?'disabled':''}>Submit two-sided quotes</button></form>${explainButton('quote_skew','quote movement')}</section>`:''}
<section class="env-section"><h2>Learn from public evidence</h2><button id="env-quiz">Quiz me on this environment</button><div id="env-quiz-output"></div><p class="env-note">Reading and hypotheses do not increase mastery. Historical movements do not establish their cause.</p><div class="env-pills">${explainButton('market_environments','the environment')}${explainButton('pnl_attribution','accounting')}</div></section>
${s.challenge?`<section class="env-section"><h2>Scenario objective</h2><p>${esc(s.challenge.objective)}</p><p>${s.challenge.success===null?'In progress':s.challenge.success?'Objective completed':'Objective not completed'}</p><p class="env-note">${esc(s.challenge.note)}</p><button data-env-action="acknowledge_liquidity" ${readonly?'disabled':''}>Record observed liquidity deterioration</button></section>`:''}
</aside></div>
${hist?`<section class="env-section"><h2>Risk and causal relationships</h2><p class="env-note">${esc(risk.label)} · ${esc(risk.horizon||risk.reason)}</p>${table(['VaR','Expected Shortfall','Revealed returns'],[[risk.var,risk.es,risk.n??0]])}${explainButton('expected_shortfall','Expected Shortfall')}<h3>Paired signal</h3><p>${s.signal.available?'Current z-score: '+esc(n(s.signal.z)):esc(s.signal.reason)}</p>${explainButton('z_score','z-score')}<details><summary>Full public signal inputs</summary>${raw(s.signal)}</details></section>`:s.engine==='statarb'?`<section class="env-section"><h2>Causal pair signal</h2><p>Current z-score: ${esc(n(c.signal?.z))}</p>${explainButton('z_score','z-score')}<p class="env-note">This existing engine requires a fitted causal relationship before individual-leg or pair trading. Reveal sufficient observations first; the default windows need 32. Current status: ${esc(c.signal?.reason||'model available')}.</p>${(c.markets||[]).map(m=>`<h3>${esc(m.instrument)} · position ${esc(m.position)}</h3>${table(['Side','Price','Quantity','Orders'],['asks','bids'].flatMap(side=>(m[side]||[]).map(p=>[side,p.price,p.quantity,p.order_count])))}`).join('')}<div class="env-controls"><button data-pair="long" ${readonly?'disabled':''}>Long spread</button><button data-pair="short" ${readonly?'disabled':''}>Short spread</button><button data-env-action="next_leg" ${readonly?'disabled':''}>Execute queued leg</button><button data-env-action="close" ${readonly?'disabled':''}>Close pair</button></div></section>`:''}
<section class="env-section"><h2>Source and assumptions</h2><p>${esc(s.environment.truth)}</p>${hist?`<details><summary>Data provenance and transformations</summary>${raw(s.provenance)}</details><details><summary>Paper-execution parameters</summary>${raw(s.execution)}</details>`:s.configuration?`<details><summary>Known scenario configuration</summary>${raw(s.configuration)}</details>`:'<p>Scenario configuration is hidden. Public prices, quantities and executions remain observable.</p>'}
${!hist?`<button id="env-reveal" ${ended?'':'disabled'}>Reveal ended scenario configuration</button><div id="env-reveal-result"></div>`:''}${ended&&s.summary?`<details><summary>Post-session public summary</summary>${raw(s.summary)}</details>`:''}</section>
<section class="env-section"><h2>Save, verify and review</h2>${ended?'<a href="/api/environments/journal" download>Download ended environment journal</a>':'<p>End the session before exporting private replay metadata.</p>'}<label class="env-field">Load an original environment journal<input type="file" id="env-replay-file" accept="application/json,.json"></label><p class="env-note">Historical replay requires the exact original imported CSV and provenance. Replay frames are read-only. A restart uses a fresh account; it cannot erase what you already know.</p><button id="env-compare" ${ended?'':'disabled'}>Compare maker policy across three scenarios</button>${hist?`<button id="env-historical-study" ${ended&&s.instruments.length===2?'':'disabled'}>Run separate historical threshold study</button>`:''}<div id="env-research">${out.research?.status==='complete'?raw(out.research):''}</div></section>`;
desk.querySelectorAll('[data-env-explain]').forEach(b=>b.onclick=()=>document.dispatchEvent(new CustomEvent('quantlab-explain',{detail:{concept:b.dataset.envExplain,selector:b.dataset.envSelector||null,lab:s.engine==='historical'?'trading':s.engine}})));
desk.querySelectorAll('[data-env-action]').forEach(b=>b.onclick=()=>act(b.dataset.envAction,b.dataset.envAction==='step'?{count:1}:b.dataset.envAction==='step_time'?{seconds:hist?60:1}:{},b.dataset.envAction==='restart'));
desk.querySelectorAll('[data-pair]').forEach(b=>b.onclick=()=>act('pair',{action:b.dataset.pair}));
desk.querySelector('#env-speed').onchange=e=>act('speed',{observations_per_second:Number(e.target.value)});
desk.querySelector('#env-order-form').onsubmit=e=>{e.preventDefault();const type=desk.querySelector('#env-order-type').value;let p={side:desk.querySelector('#env-order-side').value,quantity:Number(desk.querySelector('#env-order-quantity').value),order_type:type};if(hist||s.engine==='statarb')p.instrument=desk.querySelector('#env-order-instrument').value;if(type==='limit')p.price=desk.querySelector('#env-order-price').value;act(s.engine==='options'?'stock_order':'order',p);};
desk.querySelector('#env-cancel').onclick=()=>{const value=desk.querySelector('#env-cancel-order').value;if(value==='')return;const order=orders[Number(value)];const p={order_id:order.order_id};if(s.engine==='statarb')p.instrument=order.instrument;act(s.engine==='options'?'cancel_stock':'cancel',p);};
desk.querySelector('#env-option-form')?.addEventListener('submit',e=>{e.preventDefault();const id=desk.querySelector('#env-contract').value;const quote=c.chain.find(q=>q.id===id);act('option_order',{contract_id:id,side:desk.querySelector('#env-option-side').value,quantity:Number(desk.querySelector('#env-contracts').value),quote_revision:quote.revision});});
desk.querySelector('#env-quotes')?.addEventListener('submit',e=>{e.preventDefault();act('quotes',{bid_distance:desk.querySelector('#env-bid-distance').value,ask_distance:desk.querySelector('#env-ask-distance').value,bid_size:Number(desk.querySelector('#env-bid-size').value),ask_size:Number(desk.querySelector('#env-ask-size').value),shift:desk.querySelector('#env-shift').value});});
desk.querySelector('#env-prev')?.addEventListener('click',()=>act('replay_frame',{index:s.frame_index-1}));desk.querySelector('#env-next')?.addEventListener('click',()=>act('replay_frame',{index:s.frame_index+1}));desk.querySelector('#env-final')?.addEventListener('click',()=>act('replay_frame',{index:s.frame_count-1}));
desk.querySelector('#env-replay-file').onchange=async e=>{const file=e.target.files[0];if(file){try{await act('load_replay',{journal:JSON.parse(await file.text())},true);}catch(err){document.getElementById('env-status').textContent=err.message;}}};
desk.querySelector('#env-reveal')?.addEventListener('click',async()=>{try{const r=await command('reveal',{confirm:true});desk.querySelector('#env-reveal-result').innerHTML='<p class="env-warning">'+esc(r.label)+'</p>'+raw(r.configuration);}catch(e){desk.querySelector('#env-reveal-result').textContent=e.message;}});
desk.querySelector('#env-compare').onclick=()=>act('compare',{runs:3,seconds:10});desk.querySelector('#env-historical-study')?.addEventListener('click',()=>act('historical_research',{training_end:30,evaluation_end:Math.min(s.index,80),mode:'rolling'}));
desk.querySelector('#env-quiz').onclick=async()=>{try{showQuiz(await command('quiz'));}catch(e){desk.querySelector('#env-quiz-output').textContent=e.message;}};
}
function showQuiz(v){const target=document.getElementById('env-quiz-output'),a=v.current;if(!a){target.textContent='No question yet';return;}const q=a.question;target.innerHTML=`<div class="env-quiz"><p>${esc(q.quantlab)}</p><strong>${esc(q.prompt)}</strong><form id="env-answer">${q.options.map(o=>`<label><input type="radio" name="answer" value="${esc(o.id)}" required ${a.closed?'disabled':''}>${esc(o.text)}</label>`).join('')}<button ${a.closed?'disabled':''}>Submit answer</button></form><p role="status">${esc(a.feedback||'No answer submitted.')}</p></div>`;target.querySelector('form').onsubmit=async e=>{e.preventDefault();try{showQuiz(await command('answer',{question_id:a.id,answer:new FormData(e.target).get('answer')}));}catch(err){target.insertAdjacentHTML('beforeend','<p>'+esc(err.message)+'</p>');}};}
get().then(out=>{catalog=out;render(out);if(location.hash==='#choose-environment'||(!out.state&&location.pathname==='/'))chooser.open=true;}).catch(e=>{document.getElementById('env-status').textContent=e.message;});
setInterval(async()=>{if(busy||document.querySelector('dialog[open]')||!document.getElementById('environment-desk'))return;try{const out=await get();if(out.state)render(out);}catch{}},1200);
})();
